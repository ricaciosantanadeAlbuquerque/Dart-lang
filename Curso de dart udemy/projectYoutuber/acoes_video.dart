abstract class AcoesVideo {

  void play();
  void pause();
  void like();
  
  /**
   * Classes abstract em Dart  são usadas como interface.
   * void play(); ( Métodos abstratos são previstos  na interface, Porém são implementados, pela classe que implementa a * interface )
   */
}
