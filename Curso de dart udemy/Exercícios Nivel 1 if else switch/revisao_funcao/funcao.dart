void main(){
    /**
     * Funções numericas 
     * floor() faz o arredondamento para baixo
     * round() faz o arredondamento para o numero mais próximo
     * ceil() faz o arredondamento para cima 
     * clamp() retorna um valor mais próximo do inteiro  entre os intervalor passados
     * compareTo() compara dois valores retornando 0 se for igual ou 1 se for maior ou -1 se for menor
     * remainder() retorna o resto da divisão entre os dois valores 
     * toInt() converte para inteiro 
     * toDouble() converte para  double 
     * toString() converte para String
     * toStringAsFixed() retorna a quantidade de casa decimais que o usuário deseja
     * truncate() elimina as casas decimais 
     * isNegative este get mostra se o valor está a baixo de 0
     * isInfinite este get mostra se o valor e infinito
     * gcd() retorna o maximo de divisor comum entre os intervalor 
    */
}